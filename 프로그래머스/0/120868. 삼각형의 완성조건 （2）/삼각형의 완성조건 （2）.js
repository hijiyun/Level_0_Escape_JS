function solution(sides) {
    return Math.min(...sides) * 2 - 1

    
    // let answer = 0;
    // if (answer < sides[0] + sides[1] || answer + sides[0] > sides[1] || answer + sides[1] > sides[0]) {
    //     answer++;}
    //     return answer.toString().length;
}