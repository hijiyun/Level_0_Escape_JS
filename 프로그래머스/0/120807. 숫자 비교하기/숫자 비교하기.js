function solution(num1, num2) {
    if (num1 === num2){
        return 1;
    } else return -1;
}

// function solution(num1, num2) {
//     const result = num1 === num2 ? 1 : -1;
//     return result
// }

// const solution = (num1, num2) => num1 === num2 ? 1 : -1; 